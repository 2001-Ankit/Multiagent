# Resume folder

Drop your resume here as **PDF**, **TXT**, or **Markdown** (e.g. `resume.pdf`).

The job finder agent reads the newest supported file in this folder. To point at
a file somewhere else instead, set `RESUME_PATH` in `.env`.

Your actual resume files are gitignored for privacy — only this README is tracked.
